# Mount

A component is born!

<br />

The mounting phase is the first out of three main steps in the React component lifecycle. During the mounting phase, the
life of a component starts. The component is created and inserted into the DOM. This phase only happens once for every
component.
